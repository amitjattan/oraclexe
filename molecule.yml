---
driver:
  name: docker
  #run_options: "--shm-size=1G"
docker:
  containers:
    - name: oraclexe
      image: oraclelinux
      image_version: 7-slim
      ansible_groups:
        - group1
      #privileged: True
      #port_bindings:
      #  80: 80
verifier:
  name: testinfra
molecule:
  ignore_paths:
    - .git
    - .vagrant
    - .molecule
    - venv
